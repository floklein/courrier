# Repository Guidance

- When a Shadcn component is needed and is not already present, install it with the Shadcn CLI instead of recreating the component by hand.
- Do not add regression tests unless the user explicitly asks for them.
