
[phases.setup]
nixPkgs = ["...", "nodejs_24", "pnpm"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = [
  "pnpm --filter @courrier/mail-contracts build",
  "pnpm --filter @courrier/relay build"
]

[start]
cmd = "pnpm --filter @courrier/relay start"
